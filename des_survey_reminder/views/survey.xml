<odoo>
    <!-- Inherit Survey Form View -->
    <record id="survey_form_view_inherit" model="ir.ui.view">
        <field name="name">survey.survey.form.inherit</field>
        <field name="model">survey.survey</field>
        <field name="inherit_id" ref="survey.survey_survey_view_form"/>
        <field name="arch" type="xml">
            <xpath expr="//notebook" position="inside">
                <page string="Recipients">
                    <group>
                        <field name="partner_ids" widget="many2many_tags"/>
                    </group>
                </page>
            </xpath>
        </field>
    </record>

    <!-- Add Survey History to Partner Form -->
    <record id="view_partner_form_inherit_survey_history" model="ir.ui.view">
        <field name="name">res.partner.form.inherit.survey.history</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_partner_form"/>
        <field name="arch" type="xml">
            <xpath expr="//div[@name='button_box']" position="inside">
                <button class="oe_stat_button"
                        type="object"
                        name="action_view_surveys"
                        icon="fa-survey">
                    <field string="Surveys" name="survey_count" widget="statinfo"/>
                </button>
            </xpath>
        </field>
    </record>
</odoo>
